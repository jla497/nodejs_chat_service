stages:

  - build

  - test

  - deploy

before_script:
  - sudo rm -r -f .vagrant
  
build-server:

  stage: build

  script:
    - echo "build"
deploy-server:
  stage: deploy
  script: 
    - pwd
    - cp * ~/deploy_project
#    - sudo vagrant provision

  environment:
    name: staging
  #only:
  # - master





     
