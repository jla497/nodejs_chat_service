stages:

  - build

  - test

  - deploy

before_script:
  - sudo rm -r -f .vagrant
  
build-server:

  stage: build

  script:
    - echo "build"
deploy-server:
  stage: deploy
  script: 
    - pwd
    - cp -r public/ ~/deploy_project
    - cp package.json ~/deploy_project
    - cp server.js ~/deploy_project
    - cp bootstrap.sh ~/deploy_project
    - cd ~/deploy_project
#    - sudo vagrant provision

  environment:
    name: staging
  #only:
  # - master





     
